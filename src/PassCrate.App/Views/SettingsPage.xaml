<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage x:Class="PassCrate.App.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Settings"
             BackgroundColor="{AppThemeBinding Light={StaticResource LightBackground}, Dark={StaticResource DarkBackground}}">
    <ScrollView x:Name="SettingsScrollView">
        <VerticalStackLayout Style="{StaticResource PageContent}" MaximumWidthRequest="680">
            <VerticalStackLayout Spacing="3">
                <Label Text="MAKE IT YOURS" Style="{StaticResource AccentEyebrow}" />
                <Label Text="Settings" Style="{StaticResource PageTitle}" FontSize="27" />
                <Label Text="Control security, appearance, and encrypted cloud access."
                       Style="{StaticResource PageLead}" />
            </VerticalStackLayout>

            <Label Text="SECURITY" Style="{StaticResource Eyebrow}" Margin="0,6,0,0" />
            <Border Style="{StaticResource Card}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ChangePasswordCommand}" />
                </Border.GestureRecognizers>
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                    <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                        <Label Text="✦" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="Change vault passphrase" FontFamily="OpenSansSemibold" />
                        <Label Text="Choose a new passphrase for your vault" Style="{StaticResource Muted}" />
                    </VerticalStackLayout>
                    <Label Grid.Column="2" Text="›" Style="{StaticResource Muted}" FontSize="25" VerticalTextAlignment="Center" />
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                    <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                        <Label Text="◎" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="Biometric unlock" FontFamily="OpenSansSemibold" />
                        <Label Text="Use your fingerprint or face unlock" Style="{StaticResource Muted}" />
                    </VerticalStackLayout>
                    <Switch Grid.Column="2" IsToggled="{Binding BiometricEnabled}" VerticalOptions="Center" />
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="8">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                            <Label Text="◷" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Text="Auto-lock" FontFamily="OpenSansSemibold" />
                            <Label Text="Always locks when PassCrate leaves the app" Style="{StaticResource Muted}" />
                        </VerticalStackLayout>
                    </Grid>
                    <Border Style="{StaticResource FieldBorder}">
                        <Picker ItemsSource="{Binding AutoLockOptions}" SelectedItem="{Binding SelectedAutoLock}" />
                    </Border>
                </VerticalStackLayout>
            </Border>

            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="8">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                            <Label Text="⌁" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Text="Clipboard cleanup" FontFamily="OpenSansSemibold" />
                            <Label Text="Automatically removes copied values" Style="{StaticResource Muted}" />
                        </VerticalStackLayout>
                    </Grid>
                    <Border Style="{StaticResource FieldBorder}">
                        <Picker ItemsSource="{Binding ClipboardOptions}" SelectedItem="{Binding SelectedClipboardTimeout}" />
                    </Border>
                </VerticalStackLayout>
            </Border>

            <Label Text="APPEARANCE" Style="{StaticResource Eyebrow}" Margin="0,8,0,0" />
            <Border Style="{StaticResource Card}">
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                    <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                        <Label Text="◐" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="Theme" FontFamily="OpenSansSemibold" />
                        <Label Text="Use your phone's setting, light, or dark" Style="{StaticResource Muted}" />
                    </VerticalStackLayout>
                    <Picker Grid.Column="2" WidthRequest="120" ItemsSource="{Binding ThemeOptions}" SelectedItem="{Binding SelectedTheme}" />
                </Grid>
            </Border>

            <Label Text="BACKUP &amp; SYNC" Style="{StaticResource Eyebrow}" Margin="0,8,0,0" />
            <Border Style="{StaticResource Card}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CloudSyncCommand}" />
                </Border.GestureRecognizers>
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                    <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                        <Label Text="☁" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="Cloud sync" FontFamily="OpenSansSemibold" />
                        <Label Text="{Binding CloudStatusText}" Style="{StaticResource Muted}" />
                    </VerticalStackLayout>
                    <Label Grid.Column="2" Text="›" Style="{StaticResource Muted}" FontSize="25" VerticalTextAlignment="Center" />
                </Grid>
            </Border>
            <Border Style="{StaticResource Card}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ReviewConflictsCommand}" />
                </Border.GestureRecognizers>
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                    <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                        <Label Text="⇄" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                    </Border>
                    <Label Grid.Column="1" Text="{Binding ConflictStatusText}" FontFamily="OpenSansSemibold" VerticalTextAlignment="Center" />
                    <Label Grid.Column="2" Text="›" Style="{StaticResource Muted}" FontSize="25" VerticalTextAlignment="Center" />
                </Grid>
            </Border>

            <Label Text="HELP &amp; SUPPORT" Style="{StaticResource Eyebrow}" Margin="0,8,0,0" />
            <Border Style="{StaticResource Card}"
                    SemanticProperties.Description="Help and support. Learn how to use every PassCrate page. Double tap to open."
                    AutomationProperties.IsInAccessibleTree="True">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding HelpCommand}" />
                </Border.GestureRecognizers>
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12" MinimumHeightRequest="44">
                    <Border Style="{StaticResource IconTile}" WidthRequest="42" HeightRequest="42" StrokeShape="RoundRectangle 13">
                        <Label Text="?" Style="{StaticResource AccentIcon}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                    </Border>
                    <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                        <Label Text="Help and support" FontFamily="OpenSansSemibold" />
                        <Label Text="Learn how to use every PassCrate page" Style="{StaticResource Muted}" />
                    </VerticalStackLayout>
                    <Label Grid.Column="2" Text="›" Style="{StaticResource Muted}" FontSize="25" VerticalTextAlignment="Center" />
                </Grid>
            </Border>

            <Button Text="Lock vault now" Command="{Binding LockCommand}" Style="{StaticResource SecondaryButton}" />

            <Label Text="DANGER ZONE" Style="{StaticResource Eyebrow}" TextColor="{StaticResource Danger}" Margin="0,8,0,0" />
            <Button Text="Reset PassCrate" Command="{Binding ResetCommand}" Style="{StaticResource DangerOutlineButton}" />

            <Border Style="{StaticResource ElevatedCard}" Margin="0,8,0,0">
                <VerticalStackLayout Spacing="4">
                    <Label Text="PassCrate" FontFamily="OpenSansSemibold" />
                    <Label Text="Version 1.0 · Works offline · Optional cloud backup" Style="{StaticResource Muted}" />
                </VerticalStackLayout>
            </Border>
            <Label Text="{Binding ErrorMessage}" Style="{StaticResource ErrorText}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
